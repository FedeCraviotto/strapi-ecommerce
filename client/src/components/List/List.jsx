import React from "react";
import './list.scss';
import Card from '../Card/Card';
function List(){

    return(
        <div className="cardList">
            {/* {data?.map((item)=>(
                <Card item={item} key={item.id}/>
            ))} */}
        </div>
    )
}
export default List;